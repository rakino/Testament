# SPDX-FileCopyrightText: 2025 Hilton Chain <hako@ultrarare.space>
#
# SPDX-License-Identifier: CC0-1.0

if has guix; then
    watch_file channels.lock
    PKGS="`
        `gnupg `
        `make `
        `sops `
        `--development guix `
        `"
    eval "$(guix time-machine --channels=channels.lock -- shell --search-paths $PKGS)"
fi
